package com.richenterprises.banking_api.entity;

/**
 * This defines user roles for role-based access control (RBAC).
 * The roles are stored as strings for readability and preventing bugs from reordering or 
 * updating of roles within the enum.
 */
public enum Role {
    CUSTOMER, ADMIN

}
